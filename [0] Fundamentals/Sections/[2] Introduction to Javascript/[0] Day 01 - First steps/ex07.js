/**
7 - Use if/else to write a code that converts a percentage grade (from 0 to 100) into letter grades from A to F.

Follow the following rules:
Percentage >= 90 -> A

Percentage >= 80 -> B

Percentage >= 70 -> C

Percentage >= 60 -> D

Percentage >= 50 -> E

Percentage < 50 -> F

The code should return an error message and terminate if the grade passed is less than 0 or greater than 100.
 */